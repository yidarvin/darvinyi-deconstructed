verdict: revise

## Critique round — 2026-07-27

### Required

1. **07-hyperspace — fluorescent-tube callouts trace empty wall, not the fixtures.** In the proof, the two illuminated tubes are at the lower-right wall (roughly y 0.61–0.69), while `upper fluorescent tube` is specified from y 0.474 to 0.445 and `lower fluorescent tube` from y 0.515 to 0.487. Both polylines therefore sit materially above their named fixtures. Reposition them to the visible tubes (or remove them); the 100 geometric score does not validate the semantic polyline claims.

2. **06-heightened-realism and 07-hyperspace — source-note descriptions and per-image fair-use rationales are crossed.** Plate 06 visibly contains the clock, blinds, and foreground fronds, but `sources.md` lines 40–42 describe a tiled ceiling and fluorescent tubes; plate 07 visibly contains that tiled ceiling and the two tubes, but lines 46–48 describe the plant/clock/blinds image. This makes the per-image pedagogical-need and Factor 1/3 explanations false for both selected works. Correct the two entries so each image has a truthful, image-specific four-factor assessment.

### Advisory (non-blocking)

- **09-strip-mall-of-the-gods — `symmetry_axis` is best read as a balance axis, not literal mirroring.** The label and chapter already make that distinction, and the measured vertical correlation supports its placement, so this does not block approval. Consider a primitive that avoids the schema's literal-symmetry implication if the renderer affords one.

All twelve proofs were visually checked. Each spec has 3–4 primitives and scores 100/100 against its current analyzer JSON; the remaining overlay labels, chapter measurements, titles, credits, and camera-settings caveat are materially consistent with the visible rendered plates and gallery record.

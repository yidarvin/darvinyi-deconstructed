verdict: revise

## Critique round — 2026-08-02

### Required

1. **`09-scrap-of-evidence-apon-i` — re-bound the `green plaque` frame.** The proof shows the frame at `x: 0.36, y: 0.60, w: 0.10, h: 0.14` beginning in the empty brown field and ending through the plaque rather than enclosing the visible green stepped plaque. This makes the primitive’s named feature materially false. Resize/reposition that `frame_in_frame` to bound the plaque; retain the correctly placed plaque counterweight anchor.

### Advisory (non-blocking)

- The figure-sequence paths in `02-chronological-remuneration-xi` and `03-chronological-remuneration-xiii`, and the portrait/typebar axes in `10-scrap-of-evidence-apon` and `11-scrap-of-evidence-awalah`, are visually intelligible but receive detector-only leading-line warnings. Recasting them as interpretive polylines would better express the claim; no change is required because their labels still identify visible structures.
- `03-chronological-remuneration-xiii` places its analyzer-matched low-weight anchor at the lower edge, so its circle is clipped in the proof. It remains an accurate anchor but could be made more legible if a revision otherwise touches that plate.

All twelve current proofs were visually reviewed. Analyzer values quoted in the chapter match their analysis files within tolerance; normal overlay scores are 94 or 100. Every acquired work has a live gallery record and public display-derivative link, with a per-image four-factor fair-use rationale and credit. The chapter does not invent capture settings.

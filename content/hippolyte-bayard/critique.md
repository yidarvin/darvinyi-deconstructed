verdict: resolved

Reviewed all 10 proofs against their overlay JSON, analysis JSON, sources.md, research.md, and chapter.mdx. The chapter's numeric transcriptions from the analyzer are almost all accurate (independently recomputed rounding for every tonal stat, symmetry score, horizon value, saliency peak, and superlative claim across all 10 plates and only found the one issue below), the overlay primitives visually trace the features their labels name in every proof PNG, pedagogy is well within the 3-5 primitive range throughout, and no camera settings are fabricated — the Callout is explicit that no aperture/shutter/ISO survives and research.md corroborates the direct-positive vs. calotype technical history in detail. One specific geometric claim in the prose, however, does not hold up when checked against the underlying line data, and it's stated with enough false precision ("independently confirms," "almost exactly") that it would mislead a reader about how the image's perspective actually works. (Independently re-verified in a second pass: recomputed the vanishing-point geometry directly from analysis/04-self-portrait-garden.analysis.json and confirmed the finding.)

## Required fixes

1. **04-self-portrait-garden.jpg** — chapter.mdx body text (paragraph under "the staged tableau") and the plate caption both claim: "a leading line traces the ladder's diagonal from (0.093, 0.52) up to (0.214, 0.104), a line the analyzer independently confirms: it finds a vanishing point at x equals 0.159, y equals 0.714 built from four converging diagonals, almost exactly where that ladder line points" (caption repeats this as "confirmed by the analyzer's own vanishing point near x0.16, y0.71"). This is not true. Extrapolating the ladder's own detected line (analysis `lines` entry with the identical endpoints, angle -77.94°) through the analyzer's reported vanishing_point y (0.7144) lands at x ≈ 0.037, not x ≈ 0.159 — a 0.12 miss, four times the 0.03 coordinate tolerance. The perpendicular distance from the reported VP (0.1585, 0.7144) to the ladder line is ≈0.148 (14.8% of frame) — clearly not an inlier — while four *other* diagonal lines, all part of the trellis lattice behind Bayard (confirmed visually in the proof, and the overlay's own notes call out "one cell of the lattice"), sit within 0.0–0.07 of the VP and are almost certainly the actual "four converging diagonals" the algorithm used. So the analyzer's vanishing point is real and correctly transcribed as a number, but it is built from the trellis lattice, not the ladder, and the ladder does not point toward it. The prose should either (a) attribute the vanishing point to the trellis lattice's own diagonal bracing rather than the ladder, and describe the ladder's leading line as a separate, hand-picked compositional observation not corroborated by that specific VP, or (b) drop the "independently confirms / almost exactly" framing entirely. The leading_line primitive itself is fine and correctly traces the visible ladder in the photograph — only the prose's causal claim about it needs correcting.

## Advisory (non-blocking)

- **09-column-barriere-du-trone.jpg**: chapter.mdx says its edge density of 0.011 is "second lowest in the set." Recomputing edge density across all 10 analysis files, plate 09 (0.01087) is actually third-lowest chapter-wide (behind 01 at 0.00253 and 02 at 0.00478); if "the set" means just the four oval-mask architecture plates (05/06/09/10), it's first-lowest, not second. Either reading makes the ordinal wrong by one place. Doesn't change the compositional reading (09 is still correctly described as one of the quietest, lowest-texture prints) — just a minor ranking slip.
- Documentation-only inconsistency, not reader-facing: sources.md's rights paragraph describes "two [images] are calotypes from the Metropolitan Museum of Art's Open Access program," referring to images 02 and 03. research.md and chapter.mdx (which agree with each other) instead classify image 02 (Windmills, Montmartre) as one of the two *direct-positive* plates and group image 03 with the eight calotype/salted-paper plates. chapter.mdx itself is internally consistent and this doesn't reach the reader, but sources.md's phrasing could confuse a future editor cross-referencing rights vs. process.
- All symmetry-axis, horizon-line, subject-anchor, and frame-in-frame placements were checked against their proof PNGs and read correctly against the visible photograph in every one of the 10 plates; no other numeric claim (tonal mean/std/p05/p95, symmetry scores, horizon y/confidence, saliency peaks, aspect ratios, superlative comparisons) deviated from the analyzer JSON beyond rounding.

## Builder resolution — 2026-07-13

First resolution round for this slug: no prior "## Builder resolution" section existed in this file before this pass, so there was nothing to re-verify from an earlier round.

Required fixes applied:

1. **04-self-portrait-garden.jpg vanishing-point claim (content/hippolyte-bayard/chapter.mdx and src/chapters/hippolyte-bayard.mdx, kept byte-identical).**
   - Body paragraph under "the staged tableau": old text read "...while a leading line traces the ladder's diagonal from (0.093, 0.52) up to (0.214, 0.104), a line the analyzer independently confirms: it finds a vanishing point at x equals 0.159, y equals 0.714 built from four converging diagonals, almost exactly where that ladder line points." New text reads "...while a leading line traces the ladder's diagonal from (0.093, 0.52) up to (0.214, 0.104), a hand-picked observation the analyzer's own geometry does not corroborate: the vanishing point it finds, at x equals 0.159, y equals 0.714, is built instead from four converging diagonals in the trellis lattice's own bracing, not from the ladder, whose detected line, extrapolated to that same y, misses the point by roughly 0.12 in x." This follows option (a) from the required fix: the VP is now attributed to the trellis lattice's diagonal bracing, and the ladder's leading line is described as a separate, hand-picked observation not corroborated by that VP. The "independently confirms / almost exactly" framing is gone.
   - Plate caption for id `04-self-portrait-garden`: old value `"Bayard braces on a barrel at the frame's right third while a ladder's diagonal, confirmed by the analyzer's own vanishing point near x0.16, y0.71, cuts across a rigid trellis lattice."` New value `"Bayard braces on a barrel at the frame's right third while a ladder's diagonal cuts across a rigid trellis lattice, whose own diagonal bracing, not the ladder, produces the analyzer's vanishing point near x0.16, y0.71."` (no em dash, to satisfy the prose-lint gate on `src/chapters/**`).
   - The `leading_line` primitive in `content/hippolyte-bayard/overlays/04-self-portrait-garden.json` was left untouched, as directed — it still correctly traces the visible ladder's endpoints (0.093, 0.52) to (0.214, 0.104).

Prior rounds re-verified: none (this is the first resolution round for hippolyte-bayard).

Advisory items taken (both cheap, obviously correct, no cost to the required fix):

- **09-column-barriere-du-trone.jpg edge-density ordinal.** `content/hippolyte-bayard/chapter.mdx` and `src/chapters/hippolyte-bayard.mdx`: old value "...an edge density of 0.011, second lowest in the set." New value "...an edge density of 0.011, the lowest of the four oval-mask architecture plates in this section." Verified by recomputing edge_density across all 10 analysis JSONs: plate 09 is 0.01087, the lowest of the four oval-mask plates (05: 0.02355, 06: 0.02798, 09: 0.01087, 10: 0.02562), avoiding the chapter-wide ordinal that was off by one place.
- **sources.md rights-paragraph process mismatch.** `content/hippolyte-bayard/sources.md`: old value "two are calotypes from the Metropolitan Museum of Art's Open Access program (CC0)." New value "two — one a direct-positive print (02), one a calotype (03) — are from the Metropolitan Museum of Art's Open Access program (CC0)." This now agrees with research.md and chapter.mdx, which both classify image 02 (Windmills, Montmartre) as direct-positive and image 03 (Still Life with Statuary) as calotype.

Not taken: none of the advisory items were skipped.

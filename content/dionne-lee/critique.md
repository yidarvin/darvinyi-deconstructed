verdict: resolved

## Builder resolution — 2026-08-02

Re-verified critique history: 2026-08-02, initial revise round (the only prior
round). All nine required fixes remain present in the current overlays, proofs,
sources, and byte-identical chapter files:

1. `02-untitled-wound` now frames the actual upper red floral insert.
2. `03-true-north` now bounds and names the complete six-panel field.
3. `05-fire-bed` traces the visible reaching arm and hand.
4. `06-trespass-is-the-most-beautiful-word` bounds the inner floral insert; its
   source rationale now describes the visible hands and insert, not absent text.
5. `07-a-plot-that-also-grounds` identifies the torn opening and pointing finger.
6. `09-contact-a-muscle-memory` removes the false hand-contour trace.
7. `10-netting` bounds the complete central photographic insert.
8. `11-a-test-for-40-acres` bounds the bright mylar sheet and follows its lower
   edge.
9. `12-broken-signal` bounds all six panels; the caption and body now identify
   five luminous flares.

All nine re-rendered proofs received a clean visual pass and a 100/100 overlay
score. `scripts/check.sh` passes.

## Critique round — 2026-08-02

### Required

1. `02-untitled-wound`: retarget or relabel `RED WOUND ACCENTS`. Its ellipse (`cx=0.50`, `cy=0.53`, `rx=0.13`, `ry=0.24`) encloses mostly monochrome central collage fragments, while the clearly red elements lie outside or at its extreme edge. The label therefore names a feature the mark does not trace.

2. `03-true-north`: correct the `FOUR-PART PHOTOGRAPHIC FIELD` claim in the overlay and chapter. The frame cuts across the outer portions of the upper panels and the full collage visibly contains six photographic panels, not a four-part collage. Reframe a coherent four-panel grouping, or revise the count and accompanying caption/body text.

3. `05-fire-bed`: retarget or remove `ARM ENTERING THE GROUND FIELD`. The polyline runs below the forearm, across leaf litter and the prepared ground (`approximately y=0.38–0.55`), rather than following the arm that enters from the right higher in the frame.

4. `06-trespass-is-the-most-beautiful-word`: refit `CENTRAL FLORAL APERTURE`. The current ellipse (`x=0.21–0.81`, `y=0.17–0.87`) follows the much larger enclosing hand/surround shape; the floral aperture is the substantially smaller inner oval. Also correct the item 6 rationale in `sources.md`: it says the complete frame is needed to analyze “the text’s placement within the photograph,” but no text is visible in this plate. State the actual visible compositional need instead.

5. `07-a-plot-that-also-grounds`: correct both named features. The `ALTERED CIRCULAR PATCH` ellipse sits below the torn dark opening and outlines unrelated flowers, while `HAND ENTERING THE PLOT` follows ground below the pointing finger rather than the hand-to-opening path. Retarget, relabel, or remove each; the current marks conflict with the caption and body description.

6. `09-contact-a-muscle-memory`: retarget or remove `WHITE HAND GESTURE`. Its final segment leaves the white hand and ends in the dark upper collage area, so it does not trace a continuous hand contour as its label claims.

7. `10-netting`: refit `CENTRAL PHOTOGRAPHIC INSERT`. The frame (`x=0.34–0.68`, `y=0.51–0.71`) cuts through the visible insert and excludes substantial top/right portions; it must bound the actual central photograph or make a different, truthful claim.

8. `11-a-test-for-40-acres`: correct the `REFLECTIVE MYLAR FIELD` frame and the matching body sentence. Its top edge at `y=0.60` runs through the bright sheet instead of enclosing it; the chapter consequently falsely says the overlay frames the actual sheet. Reframe the sheet boundary or omit this primitive while retaining the valid lower-edge trace.

9. `12-broken-signal`: expand or refit `SIX-PART SIGNAL FIELD` so it contains the complete six-panel field rather than clipping its outer panels. Correct the caption/body statement that there are “six luminous interruptions”: the proof shows six panels but five visible luminous flares, with the lower-central panel dark.

### Advisory (non-blocking)

- `01-breaking-wave`: `TORN IMAGE SEAM` works as a legible directional cue but only loosely follows the torn inner edges.
- `10-netting`: `WOVEN DIAGONAL TRACE` crosses several graphite marks rather than one continuous trace; it remains a readable indication of the woven field.
- `12-broken-signal`: `UPPER SIGNAL ROW` runs through panel interiors rather than precisely along a row boundary, but the indicated upper grouping remains clear.

All twelve proofs were visually checked against their current specs and analyses. The cited Aperture, MoMA, NOMA, and Strange Fire records are traceable; the dossier provides a concise four-factor fair-use rationale per image, and no invented capture settings were found. The chapter's measured tonal, palette, grid, and edge-density claims match the stored analyses within tolerance.

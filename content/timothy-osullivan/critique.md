verdict: approve

This chapter is exceptionally well fact-checked: every overlay primitive coordinate I cross-referenced against overlays/*.json matches the chapter.mdx prose to within rounding, and nearly every numeric claim in the prose (symmetry scores and axes, horizon y/angle/confidence, saliency peaks, thirds-grid fit scores, vanishing-point coordinates/inliers/confidence, tonal mean/std/p05/p95, edge density, and superlative claims like "highest in the chapter" or "lowest of the eleven plates") checks out exactly against the analysis/*.json ground truth across all 11 plates. All 11 proof PNGs were viewed at full resolution; every primitive traces the feature its label names (verified by pixel-level crop inspection of the four figures in the Zuni mission doorway, and the officer/rider cluster in Harvest of Death), primitive counts per image sit in the pedagogical 3-5 range, and the "no grid" treatment of the frontal Inscription Rock document shot is correctly the only ungridded plate, matching the prose. sources.md and research.md hold up: rights basis is clean U.S. government work, and the Callout in chapter.mdx explicitly and correctly declines to invent any f-stop/shutter/ISO value, consistent with wet-plate collodion having no per-frame exposure record — no fabricated camera settings anywhere. Biographical and technique claims (Brady/Gardner apprenticeship, King's Fortieth Parallel and Wheeler surveys, Black Cañon boat descent, 1882 death) are standard, well-documented O'Sullivan history.

## Advisory (non-blocking)

1. **06-tufa-domes**: the chapter's claim that the "paired mid-lake domes" ellipse (cx 0.49, cy 0.50) sits "close to the analyzer's own raw saliency peak at x equals 0.43, y equals 0.62" is a bit generous — the quoted peak coordinates themselves are correct, but the actual distance between the ellipse and that peak (~0.13 normalized, well outside the 0.03 tolerance) is closer to "same general area" than "close to." Doesn't change the compositional reading (the paragraph's real point, that the hand-placed subject anchor on Pyramid Rock is deliberately *not* near the saliency peak, is separately correct and precisely quoted).
2. **03-ancient-ruins-chelle**: the subject anchor (0.37, 0.525) is described as landing "almost exactly" on the analyzer's raw saliency peak (0.335, 0.523) — the x offset is 0.035, just outside the 0.03 tolerance band. Visually and compositionally trivial, but "almost exactly" slightly overstates the precision.
3. **11-zuni-mission**: chapter prose describes the mission ellipse as "0.19 wide by 0.155 tall," but those are the overlay's `rx`/`ry` (radius) values — the rendered ellipse is actually ~0.38 wide by ~0.31 tall. A minor terminology slip (radius vs. full width/height), not visible or misleading in the rendered proof itself.
